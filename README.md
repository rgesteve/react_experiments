Small playground to test react (especially recent features like hooks
and context) outside of a VSCode view environment.

Try:
```
npm install
```
and then, to run
```
npm start
```
will start the application in localhost:3000


